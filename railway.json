{
  "$schema": "https://railway.app/railway.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn item.wsgi"
  }
}
